import { CheckBox } from "devextreme-react";
import React from "react";

export default function CheckBoxCell(cellData) {
    console.log(cellData);
    return (
        <div>
            <CheckBox></CheckBox>
        </div>
    );
}
